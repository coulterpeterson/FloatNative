{
  "name": "floatnative",
  "private": true,
  "scripts": {
    "openapi:generate:swift": "pnpm --filter @floatnative/openapi generate:swift",
    "openapi:generate:kotlin": "pnpm --filter @floatnative/openapi generate:kotlin",
    "openapi:generate:all": "pnpm --filter @floatnative/openapi generate:all",
    "openapi:update-spec": "pnpm --filter @floatnative/openapi update-spec",
    "api:dev": "pnpm --filter api dev",
    "api:deploy": "pnpm --filter api deploy",
    "api:deploy:non-production": "pnpm --filter api deploy:non-production",
    "api:tail": "pnpm --filter api tail",
    "api:db:generate": "pnpm --filter api db:generate",
    "api:db:migrate:local": "pnpm --filter api db:migrate:local",
    "api:db:migrate:remote": "pnpm --filter api db:migrate:remote"
  }
}